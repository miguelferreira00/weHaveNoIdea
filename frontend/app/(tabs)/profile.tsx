import { View, Text, Image, Pressable, StyleSheet, Animated, Alert } from 'react-native';

export default function Profile() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#FFF2F2' }}>
            <Text style={{ fontSize: 24 }}>Profile Screen is under development</Text>
        </View>
    );
}